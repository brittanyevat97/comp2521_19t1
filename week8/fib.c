#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

int fibR(int n, int *knownFib) {
    // TODO
}

int fib(int n) {
    assert(n > 0);
    int *knownFib = calloc(n+1, sizeof(int));
    assert(knownFib != NULL);

    // TODO
}
